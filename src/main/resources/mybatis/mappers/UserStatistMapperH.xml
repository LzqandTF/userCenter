<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yijiawang.web.platform.userCenter.dao.UserStatistMapper" >

    <select id="getUserStatist" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from user_statist
        where userId = #{userId,jdbcType=VARCHAR} AND role = #{role,jdbcType=INTEGER} AND type=#{type,jdbcType=INTEGER}
    </select>

    <update id="addUserStatist">
        update user_statist
        set
        count = count + 1,
        updateTime = CURRENT_TIMESTAMP
        userId = #{userId,jdbcType=VARCHAR} AND role = #{role,jdbcType=INTEGER} AND type=#{type,jdbcType=INTEGER}
    </update>
</mapper>